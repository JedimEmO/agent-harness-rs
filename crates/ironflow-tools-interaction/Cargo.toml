[package]
name = "ironflow-tools-interaction"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "User interaction tools for ironflow agents (ask_user, offer_options, confirm_action, show_plan)"

[dependencies]
ironflow-core = { workspace = true }
async-trait = { workspace = true }
serde_json = { workspace = true }
